import { Playfair_Display, Roboto, Open_Sans } from "next/font/google";

export const playfair = Playfair_Display({
  subsets: ["latin"],
  display: "swap",
});

export const roboto = Roboto({
  subsets: ["latin"],
  display: "swap",
});

export const open = Open_Sans({
  subsets: ["latin"],
  display: "swap",
});
